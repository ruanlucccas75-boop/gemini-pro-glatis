version: '3.8'

services:
  gemini-app:
    build: .
    container_name: gemini-app
    ports:
      - "3000:3000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    restart: unless-stopped
